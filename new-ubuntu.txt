# open up firefox -> search Google chrome -> download chrome -> right click on the deb file and install. -> open chrome and sync main account.

Install Dracula theme for the termimal. Download it from dracula theme website.


Commands:



sudo apt-get update && sudo apt-get upgrade -y
sudo apt install ubuntu-restricted-extras
sudo add-apt-repository universe
sudo apt install snapd
sudo apt install vlc
sudo apt install caprine
sudo apt install curl
sudo apt install python3
sudo snap install whatsdesk
sudo apt install gparted
sudo apt install gimp
sudo apt install g++
sudo apt install gnome-tweaks
sudo apt install git
sudo apt install bitwarden
sudo apt install synaptic
gsettings set org.gnome.shell.extentions.dash-to-dock click-action 'minimize'
sudo snap install vim
sudo apt install vs-code
sudo snap install discord
sudo apt install neofetch
sudo apt install chromium
sudo apt-get install openjdk-11-jdk
sudo fc-cache -f -v  // This will rebuild the system font cache. DO THIS AFTER CREATING A .font FOLDER AND PASTE THE FONTS INTO THAT.
			            


Extentions:



blur my shell
compiz alike magic lamp effect
clipboard indicator
compiz alike windows effect
open weather 
user themes
vitals




keyboard Shortcut:



windows key + Enter = Gnome-terminal
windows key + f = files
windows key + d = google chrome




GITHUB:



make a directory at home folder -> mkdir .gitconfig . then Add the lines below(without comment)

open a terminal in that directory. command ->  git remote add origin [URL of git] 
//After creating a new repo or to use the existant reporjust add it. push after you write your code.



//
[user]
	email = amihasan420@gmail.com
[credential]
	helper = cache
//




To push Code to git. Steps:

1. gii init
2. git add --a     // to add all the files in the directory
3. git add [name of the file]
4. git commit -m "why did you commit?"
5. git commit -a -m "why did you commit?" // to skip staging time
6. git push -u origin master OR git push 


